#include <stdio.h>
#define incre(x)  
int main(void)
{
    char des[] = "Hi,how are you?";
    int x = 12;
    printf("%s.Will this work or What?\n",des);
    printf("Or will the value of x be %i.", x);
    return (0);
}
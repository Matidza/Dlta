#include <stdio.h>
/**
 * main: entry point
 * Description: Add two #'s with one variable
 * return: Always return 0 (successful)
*/
int main(void)
{
    int sum = 20 + 30;
    printf("The sum of 30 and 20 is =  %i\n", sum);
    return (0);
}
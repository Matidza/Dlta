#include <stdio.h>
int main(void)
{
    int x = 12;
    int y = 32;
    int z = 9;
    int square = x* 2;
    if (x < y) {
        printf("x is less than y.");
    }
    else {
        printf("x is greater than y.");
    }
    return (0);
}
// preproccessor
#include <stdio.h>

// main function
int main(void)
{
	// variable declaration
	int m = 0;

	// executable statement
	for (m = 0; m <= 6; m++)
	{
		printf("hell0\n");
		printf("%i", m);
	}
	// return statement
	return (0);
}
#include <stdio.h>
int main(void)

{
	int i = 0;
	printf("\n");
	for (i=0;  i<6; i++) // i=0(count from zero, i<6(six outputs), i++(count from zero to positive values)
	{
		printf("How are you this morning.\n");
	}
	return (0);
}
